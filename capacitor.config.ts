import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gly.ip_rgr',
  appName: 'ip_rgr',
  webDir: 'dist/ip_rgr',
  server: {
    androidScheme: 'http'
  }
};

export default config;
